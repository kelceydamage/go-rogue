package events

/*
// TODO
var EventRegistry = map[maps.NodeType]func(loader *utilities.EventTextLoader, theme string) Event{
	maps.StartNode: func(loader *utilities.EventTextLoader, theme string) Event {
		return &NarrativeEvent{text: loader.GetText(theme, "StartNode")}
	},
	maps.EndingNode: func(loader *utilities.EventTextLoader, theme string) Event {
		return &NarrativeEvent{text: loader.GetText(theme, "EndingNode")}
	},
	maps.DecisionNode: func(loader *utilities.EventTextLoader, theme string) Event {
		return &NarrativeEvent{text: loader.GetText(theme, "DecisionNode")}
	},
	maps.EncounterNode: func(loader *utilities.EventTextLoader, theme string) Event {
		return &EncounterEvent{text: loader.GetText(theme, "EncounterNode")}
	},
	maps.SceneryNode: func(loader *utilities.EventTextLoader, theme string) Event {
		return &NarrativeEvent{text: loader.GetText(theme, "SceneryNode")}
	},
	maps.DeadEndNode: func(loader *utilities.EventTextLoader, theme string) Event {
		return &NarrativeEvent{text: loader.GetText(theme, "DeadEndNode")}
	},
}

var TraversalRegistry = map[maps.EdgeType]func(loader *utilities.TraversalTextLoader, theme string) string{
	maps.Path: func(loader *utilities.TraversalTextLoader, theme string) string {
		return loader.GetText(theme, "Path")
	},
	maps.Crossing: func(loader *utilities.TraversalTextLoader, theme string) string {
		return loader.GetText(theme, "Crossing")
	},
	maps.Tunnel: func(loader *utilities.TraversalTextLoader, theme string) string {
		return loader.GetText(theme, "Tunnel")
	},
	maps.UnlockedDoor: func(loader *utilities.TraversalTextLoader, theme string) string {
		return loader.GetText(theme, "UnlockedDoor")
	},
	maps.LockedDoor: func(loader *utilities.TraversalTextLoader, theme string) string {
		return loader.GetText(theme, "LockedDoor")
	},
	maps.HiddenDoor: func(loader *utilities.TraversalTextLoader, theme string) string {
		return loader.GetText(theme, "HiddenDoor")
	},
}
*/
